# Integer literals are typed as COUNT in this profile.

: PARSE WORD DEFINE COLON IMMEDIATE STATE INTERPRET ( -- )
; CONTROL END IMMEDIATE STATE COMPILE ( -- )
LITERAL INTEGER ( -- count )
* ( count count -- count )
. ( value -- )
